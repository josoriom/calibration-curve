"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.matrixCreateEmpty = void 0;
const utils_1 = require("../utils");
/**
 * Create a new matrix based on the size of the current one or by using specific dimensions.
 */
function matrixCreateEmpty(options) {
    const { matrix, nbRows = matrix?.length || 1, nbColumns = matrix?.[0].length || 1, ArrayConstructor = Float64Array, } = options;
    const newMatrix = [];
    for (let row = 0; row < nbRows; row++) {
        newMatrix.push((0, utils_1.createNumberArray)(ArrayConstructor, nbColumns));
    }
    return newMatrix;
}
exports.matrixCreateEmpty = matrixCreateEmpty;
//# sourceMappingURL=matrixCreateEmpty.js.map